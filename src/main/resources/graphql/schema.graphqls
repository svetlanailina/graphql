type Query {
    getAllUsers: [User]
    getUserById(id: String!): User
}

type Mutation {
    createUser(name: String!, age: Int!): User
}

type User {
    id: String!
    name: String!
    age: Int!
}
